# Evidencias — Sprint 07

Este directorio reúne la validación de la base operativa del backend.

## Archivos esperados

| Archivo | Evidencia |
| --- | --- |
| `01-estructura-repositorio-github.png` | Estructura y archivos publicados en la rama `sprint-7` de GitHub |
| `02-fastapi-ejecucion.png` | Contenedor y logs de FastAPI en ejecución |
| `03-healthcheck.png` | Respuesta exitosa del endpoint de diagnóstico |
| `04-env-example-github.png` | Archivo `.env.example` publicado en la rama `sprint-7` |
| `logs/docker-compose.log` | Registro textual del levantamiento |
| `logs/validacion-endpoints.txt` | Estado HTTP y JSON de las rutas base |

El archivo `.env.example` se mantiene en la raíz del repositorio para que sea
versionado sin exponer credenciales reales. El archivo local `.env` está
excluido mediante `.gitignore` y no se publica.
