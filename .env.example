# Aplicación
APP_NAME=Contact Center API
APP_VERSION=0.3.0
APP_ENV=development
APP_DEBUG=true

# Servidor
APP_HOST=0.0.0.0
APP_PORT=8000

# Proveedor de embeddings: local u openai
EMBEDDING_PROVIDER=local

# Embeddings locales
LOCAL_EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
LOCAL_EMBEDDING_DEVICE=cpu
LOCAL_EMBEDDING_BATCH_SIZE=16
SENTENCE_TRANSFORMERS_HOME=/models/sentence-transformers

# OpenAI opcional (no subir credenciales reales)
OPENAI_API_KEY=
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_CHAT_MODEL=gpt-4o-mini
OPENAI_TIMEOUT_SECONDS=30
OPENAI_MAX_RETRIES=2

# LLM del orquestador: mock u openai
LLM_PROVIDER=mock
LLM_TIMEOUT_SECONDS=45
LLM_MAX_RETRIES=2

# Umbrales del orquestador
RAG_MIN_SIMILARITY=0.45
RAG_MIN_RESULTS=1

# ChromaDB
CHROMA_HOST=chroma
CHROMA_PORT=8000
CHROMA_HOST_PORT=8001
CHROMA_COLLECTION_PREFIX=contact-center-documents
